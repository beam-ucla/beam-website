language: ruby
cache: bundler
jobs:
  include: 
  - name: "Building on stable RVM"
    rvm: 2.6.3
    script: bundle exec jekyll build
    deploy: 
      provider: pages
      skip_cleanup: true
      github_token: $github_token
      local_dir: _site
      on:
        branch: master